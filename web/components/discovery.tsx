'use client';
import { useState } from 'react';
import { ArrowUpRight, Compass, Heart, MapPin, SlidersHorizontal, UserRound, X, Bookmark, MoveUpRight, RotateCcw, ArrowRight, BriefcaseBusiness } from 'lucide-react';
import { demoJobs } from '@/lib/demo';
import { Tabs, TabsList, TabsTrigger, TabsContent } from '@/components/ui/tabs';
export default function Discovery(){
 const [index,setIndex]=useState(0); const job=demoJobs[index%demoJobs.length];
 return <div className="app-shell"><header className="masthead"><a className="wordmark" href="/">jobdiscover<span className="wordmark-dot">.</span></a><span className="edition">DE NOUVELLES PERSPECTIVES</span><button className="avatar" aria-label="Mon profil"><UserRound size={19}/></button></header><Tabs defaultValue="discover"><TabsList className="main-nav"><TabsTrigger value="discover"><Compass/>Découvrir</TabsTrigger><TabsTrigger value="saved"><Heart/>Favoris</TabsTrigger><TabsTrigger value="profile"><UserRound/>Profil</TabsTrigger></TabsList><TabsContent value="discover"><div className="workspace"><aside className="intro"><span className="eyebrow">UN PAS VERS LA SUITE</span><h1>Et si ton prochain<br/>travail te <em>surprenait ?</em></h1><p>Des pistes à explorer, à ton rythme.<br/>Tu n’as pas besoin d’avoir tout décidé.</p><div className="aside-note"><span className="tiny-label">LE PRINCIPE</span><p>Ce qui t’attire nous guide.<br/>La curiosité fait le reste.</p><MoveUpRight size={30} strokeWidth={1}/></div></aside><section className="discovery-column" aria-label="Offres à découvrir"><div className="section-heading"><div><span className="eyebrow">TA SÉLECTION</span><h2>À découvrir aujourd’hui</h2></div><button className="icon-button" aria-label="Ouvrir les filtres"><SlidersHorizontal size={20}/></button></div><div className="filter-summary"><span><MapPin size={14}/> Lyon · 30 km</span><span>Tous les contrats</span></div><article className="job-card" key={job.id}><div className="card-ribbon"><span><Compass size={16}/> UNE DÉCOUVERTE</span><span>0{index+1}</span></div><div className="job-body"><div className="company-line"><span className="company-monogram">{job.company.split(' ').slice(0,2).map(x=>x[0]).join('')}</span><div><p>{job.company}</p><span>{job.sector}</span></div><Bookmark className="bookmark-outline" size={21}/></div><h3>{job.title}</h3><p className="location"><MapPin size={15}/> {job.city}<span>•</span>{job.contract}</p><p className="job-summary">{job.summary}</p><div className="salary"><BriefcaseBusiness size={16}/>{job.salary}</div><div className="why"><span className="why-icon"><MoveUpRight size={19}/></span><div><strong>Et si tu explorais cette piste ?</strong><p>Une première idée pour découvrir ce qui te plaît. Tes réactions affineront la suite.</p></div></div><button className="text-button">Découvrir les missions <ArrowUpRight size={18}/></button></div></article><div className="reaction-bar"><button onClick={()=>setIndex(index+1)} className="reaction reject"><span><X/></span>Pas pour moi</button><button onClick={()=>setIndex(index+1)} className="reaction maybe"><span><Bookmark/></span>Peut-être</button><button onClick={()=>setIndex(index+1)} className="reaction like"><span><Heart/></span>Ça me plaît</button></div><div className="below-actions"><button onClick={()=>setIndex(Math.max(0,index-1))}><RotateCcw size={14}/>Annuler</button><span>Aucune réponse n’est définitive.</span></div><p className="demo-note">Aperçu · Offres fictives, aucune candidature</p></section><aside className="right-note"><span className="vertical-rule"/><span className="eyebrow">GARDE L’ESPRIT OUVERT</span><p>Le bon métier n’est pas toujours celui auquel on pense.</p><button className="text-button" onClick={()=>setIndex(index+3)}>Surprends-moi <ArrowRight size={16}/></button></aside></div></TabsContent><TabsContent value="saved"><div className="simple-page"><h1>Les pistes à garder.</h1><p>Les offres qui te plaisent seront réunies ici.</p></div></TabsContent><TabsContent value="profile"><div className="simple-page"><h1>Ce qui compte pour toi.</h1><p>Ton profil et tes préférences restent privés.</p></div></TabsContent></Tabs><footer className="desktop-footer"><span>Un peu de curiosité. De nouvelles possibilités.</span><a href="/confidentialite">Confidentialité</a></footer></div>;
}
