export type Tool={name:string;title:string;description:string;inputSchema:object;annotations:{readOnlyHint:boolean;untrustedContentHint:boolean};execute:(input:unknown)=>unknown|Promise<unknown>};
export type ModelContext={registerTool:(tool:Tool,options:{signal:AbortSignal})=>void|Promise<void>};
export function registerDiscoveryTools(context:ModelContext|undefined,read:()=>unknown,navigate:(tab:string)=>void){if(!context)return()=>{};const lifecycle=new AbortController();const tools:Tool[]=[{name:'read_current_job',title:'Lire la piste affichée',description:'Read the currently displayed job and its explanation. Does not expose account details or change feedback.',inputSchema:{type:'object',properties:{},additionalProperties:false},annotations:{readOnlyHint:true,untrustedContentHint:true},execute(input){if(!input||typeof input!=='object'||Array.isArray(input)||Object.keys(input).length)throw new Error('Expected an empty object.');return read();}},{name:'navigate_jobdiscover',title:'Ouvrir une vue',description:'Navigate to discovery, saved offers or profile in this app. Does not save, delete or submit anything.',inputSchema:{type:'object',properties:{view:{type:'string',enum:['discover','saved','profile']}},required:['view'],additionalProperties:false},annotations:{readOnlyHint:false,untrustedContentHint:false},async execute(input){if(!input||typeof input!=='object'||Array.isArray(input)||Object.keys(input).length!==1||!['discover','saved','profile'].includes((input as {view:string}).view))throw new Error('Unknown view.');const view=(input as {view:string}).view;navigate(view);await new Promise<void>(resolve=>requestAnimationFrame(()=>resolve()));return {view};}}];for(const t of tools){try{void Promise.resolve(context.registerTool(t,{signal:lifecycle.signal})).catch(()=>{});}catch{}}return()=>lifecycle.abort();}
